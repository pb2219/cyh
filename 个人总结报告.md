# 个人实训总结报告

**项目名称**: 个人记账助手 (PBCXCYH)
**姓名**: [请填写]
**日期**: 2026年7月17日

---

## 一、项目概述
本次实训我独立完成了一款基于 uni-app 框架的个人记账助手应用。项目采用前后端分离架构，前端使用 uni-app (Vue3) 跨平台框架开发 4 个独立页面，后端基于 uniCloud 阿里云 serverless 平台，编写了 6 个云函数作为 RESTful 风格的 API 接口，数据库使用 MongoDB。该项目实现了从记账到统计分析的完整业务闭环，最终可部署为 H5 网页或小程序。

## 二、遇到的最大困难及解决过程

**最大困难：uniCloud 云函数与前端的数据交互调试**

由于之前没有使用过 uniCloud 阿里云平台，在开发初期遇到了几个棘手的问题。

首先是云函数的参数校验与数据库查询语法。uniCloud 的 `db.command` 与传统 MongoDB 语法有差异，比如日期范围查询中，我需要用 `gte/lte` 来筛选当月账单。在尝试使用 `db.command.aggregate.gte` 时出现了运行时错误。通过仔细阅读 uniCloud 官方文档和多次调试，最终确认应使用 `db.command.gte().and(db.command.lte())` 的链式调用方式。

其次是云函数数据返回格式的统一问题。我一开始没有规范错误处理，导致前端需要处理多种不同的返回格式。后来设计了统一的 `{ code, data, message }` 标准返回格式，前端只需根据 `code` 判断成功或失败，大大减少了代码耦合。

还有就是在首次加载分类数据时，如果数据库中没有分类记录，会导致记账页面无法选择分类。我通过在前端增加"默认分类初始化"逻辑解决了这个问题——当检测到分类列表为空时，自动调用 `add-category` 云函数批量插入预设的 9 个常用分类。

## 三、对 AI 辅助编程的心得体会

通过本次实训全程使用 AI 辅助编程，我有以下深刻体会：

1. **AI 是高效的"编程搭档"，但不是"替代者"**。AI 可以快速生成代码框架和模板，帮我节省了大量编写重复代码的时间。但 AI 生成的代码需要人工审查和调试，很多细节（如云函数语法、平台 API 差异）需要开发者自行判断和修正。

2. **Prompt 的质量决定了 AI 输出的质量**。我发现将需求描述得越具体、越结构化，AI 返回的代码就越精准。比如指定返回格式、错误码、UI 风格等细节，能显著减少后续修改工作量。

3. **AI 辅助提升了我的学习效率**。面对不熟悉的 uniCloud 平台，我可以直接向 AI 提问 API 用法和最佳实践，避免了在海量文档中盲目搜索，学习曲线明显缩短。

4. **传统的编程能力仍然是基础**。AI 生成的代码有时存在逻辑漏洞或不符合项目规范，需要开发者具备代码审查和问题定位的能力。比如 AI 生成的某个页面缺少 `onShow` 生命周期来刷新数据，我需要自己判断并补充。

## 四、个人在项目中的具体贡献

本项目由我个人独立完成，具体贡献如下：

- **需求分析**：确定了"个人记账"作为项目选题，规划了 4 个页面和 6 个 API 的功能设计
- **架构设计**：设计了 uni-app + uniCloud 的技术架构，确定了前端 4 个 tabBar 页面和后端 2 个数据库集合、6 个云函数的整体方案
- **后端开发**：编写了全部 6 个云函数（add-bill、get-bills、get-statistics、delete-bill、get-categories、add-category），包含参数校验、错误处理和统一返回格式
- **前端开发**：完成了 4 个页面的 UI 设计和功能实现，包括月度概览、记账表单、账单列表筛选、分类管理等
- **数据库设计**：设计了 bills 和 categories 两个集合的 Schema，包含字段类型、校验规则和权限配置
- **文档编写**：编写了 README.md（包含项目介绍、安装指南、API 文档）、prompt_log.md（AI 对话记录）和个人总结报告
- **联调测试**：完成了前后端数据联调，测试了各接口的正常响应和异常场景处理

## 五、总结与展望

本次居家自学实训让我深刻认识到，在 AI 时代，软件开发的核心竞争力正在从"写代码的速度"转向"工程化思维和问题解决能力"。AI 可以帮助我们更快地写出代码，但"要做什么、怎么做、如何保证质量"这些关键决策仍然需要人来把控。

通过这个项目，我不仅掌握了 uni-app 跨平台开发和 uniCloud serverless 架构的实践技能，更重要的是学会了如何高效地与 AI 协作——把 AI 当作一个 24 小时在线的编程助手，用清晰的 Prompt 引导它，用专业的眼光审视它的产出，最终交付一个高质量、可运行的完整项目。

未来我希望在以下方面进一步学习：引入 CI/CD 自动部署流程、编写单元测试保证代码质量、使用环境变量管理配置，以及探索更多 AI 编程工具的高级用法。
